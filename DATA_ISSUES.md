# Data issues report

> **Generated by `npm run pipeline`** from the `data_issues` table — every count, line
> number and example below comes from the same run that built the database, so this
> report cannot drift from what the code actually does. The explanations are written
> by hand in `pipeline/report.js`.

**261 issues** across **22 distinct types**, found in 102 source rows
that merged into **55 people**.

| Severity | Count | Meaning |
|---|---:|---|
| High | 34 | Would corrupt data or lose a person if unhandled |
| Medium | 32 | Requires a judgement call that could reasonably go the other way |
| Low | 195 | Cosmetic or formatting, safe to normalise |

Full row-level detail: [`reports/data_issues.csv`](reports/data_issues.csv).
Cases the pipeline refused to decide: [`reports/review_queue.json`](reports/review_queue.json).

---

## High severity

### 1. Two different units in one salary column

`ctc_mixed_units` · **21** occurrences

**The problem.** source1 `Current CTC` contains both `417964` (absolute rupees per year) and `4.2` (lakhs per annum) with no marker distinguishing them. Averaging this column raw gives a meaningless number.

**What I did.** Disambiguated by magnitude - nobody earns Rs 4.2 a year and nobody earns 417,964 lakhs. Values below 100 are treated as LPA and multiplied by 100,000. The threshold is an assumption, so **every** such conversion is logged rather than applied silently.

| Where | Value | Action |
|---|---|---|
| `source1:6 · Current CTC` | `4.2` | Interpreted as 4.2 LPA -> 420000 INR/year |
| `source1:8 · Current CTC` | `8.3` | Interpreted as 8.3 LPA -> 830000 INR/year |
| `source1:14 · Current CTC` | `5.1` | Interpreted as 5.1 LPA -> 510000 INR/year |

_18 more in `reports/data_issues.csv`._

### 2. Dates where the day/month order cannot be determined

`date_ambiguous` · **8** occurrences

**The problem.** source1 mixes five date formats, and the two separators disagree about field order. `03-07-2026` could be 3 July or 7 March - a four-month error.

**What I did.** The file proves the rule both ways: `07/13/2026` can only be MM/DD (no 13th month) and `24-07-2026` can only be DD-MM (no 24th month). So the **separator** carries the convention: slash = US order, dash = day-first. Values where both parts are <= 12 remain genuinely undecidable from the data, so they are flagged at high severity rather than quietly converted.

| Where | Value | Action |
|---|---|---|
| `source1:4 · Applied Date` | `01-08-2026` | Read as DD-MM-YYYY -> 2026-08-01 |
| `source1:12 · Applied Date` | `07/03/2026` | Read as MM/DD/YYYY -> 2026-07-03 |
| `source1:22 · Applied Date` | `03-07-2026` | Read as DD-MM-YYYY -> 2026-07-03 |

_5 more in `reports/data_issues.csv`._

### 3. Columns rotated by one position

`column_shift` · **1** occurrence

**The problem.** One row in source2 has its fields shifted right by one, so the skills list sits in the email column and every other value is off by one. Ingested as-is it would create a person whose email address is "react, javascript, mysql".

**What I did.** Detected structurally, not by matching the known bad string: column 0 is not an email but a later column is, which can only happen if the fields shifted. The row is rotated back by that offset. Repairing it is what then reveals it to be a duplicate of row 7.

| Where | Value | Action |
|---|---|---|
| `source2:20` | `react, javascript, mysql\|ISHA.CHOPRA95@MAILTEST.EXAMPLE.ORG\|Isha Cho` | Rotated fields back into header order -> ISHA.CHOPRA95@MAILTEST.EXAMPLE.ORG\|Isha Chopra\| |

### 4. One person, two rows, written under different names

`duplicate_person_same_email` · **1** occurrence

**The problem.** "R. Verma" and "Rohit Verma" are the same human with the same email address and phone number. A name-based dedupe would keep both.

**What I did.** Merged on the shared email. The fuller name wins the golden record, and the abbreviated form is preserved in person_sources.

| Where | Value | Action |
|---|---|---|
| `source1:31` | `R. Verma / Rohit Verma` | Merged into one person on the shared email address |

### 5. Byte-identical duplicate row

`duplicate_row_in_source` · **1** occurrence

**The problem.** The same record appears twice in one file.

**What I did.** Dropped after trimming and lower-casing every field. Only exact repeats are handled here; anything needing judgement is left to the matcher so it can be logged with a confidence level.

| Where | Value | Action |
|---|---|---|
| `source2:20` | `react, javascript, mysql\|ISHA.CHOPRA95@MAILTEST.EXAMPLE.ORG\|Isha Cho` | Row dropped as a duplicate of row 7 |

### 6. Header line repeated inside the data

`repeated_header` · **1** occurrence

**The problem.** source3 has its header row printed again halfway down the file, a classic symptom of two exports concatenated. Ingested naively it becomes a person called "Name" with the phone number "Phone Number".

**What I did.** Any row whose joined values equal the header is dropped.

| Where | Value | Action |
|---|---|---|
| `source3:16` | `Name\|Phone Number\|City\|Verified\|Projects Completed` | Row dropped |

### 7. One person holding two email addresses

`same_person_two_emails` · **1** occurrence

**The problem.** The single most instructive record in the dataset: the same human appears twice in source1 with `nikhil.chopra70@example.com` and `alt.nikhil.chopra70@example.com`, identical in every other field. Matching on email alone produces two people.

**What I did.** Caught by the phone tier. Both addresses are kept in person_identifiers so either one still resolves to this person; the unprefixed address becomes primary.

| Where | Value | Action |
|---|---|---|
| `source1:37` | `alt.nikhil.chopra70@example.com / nikhil.chopra70@example.com` | Merged on the shared phone number; both addresses kept in person_identifiers |

## Medium severity

### 8. Two different bases in one rate column

`rate_mixed_units` · **14** occurrences

**The problem.** source2 `rate` mixes `1415/hr` and `15k/month`. They are not comparable.

**What I did.** Normalised to an hourly figure assuming 176 working hours per month (22 days x 8h). Because that divisor is an assumption and not a fact in the data, the original string is preserved in `rate_raw` alongside the computed value.

| Where | Value | Action |
|---|---|---|
| `source2:6 · rate` | `15k/month` | Converted 15000 INR/month -> 85.23 INR/hour |
| `source2:10 · rate` | `72k/month` | Converted 72000 INR/month -> 409.09 INR/hour |
| `source2:14 · rate` | `28k/month` | Converted 28000 INR/month -> 159.09 INR/hour |

_11 more in `reports/data_issues.csv`._

### 9. Email addresses in upper case

`email_case` · **9** occurrences

**The problem.** Roughly a third of source2 stores addresses in caps. Email local-parts are technically case-sensitive per RFC 5321, but no real provider treats them that way, and here they are unambiguously the same people. Left as-is, the email join misses them entirely.

**What I did.** Lower-cased for matching and storage.

| Where | Value | Action |
|---|---|---|
| `source2:7 · email_id` | `ISHA.CHOPRA95@MAILTEST.EXAMPLE.ORG` | Lower-cased to isha.chopra95@mailtest.example.org |
| `source2:13 · email_id` | `VARUN.SAXENA21@EXAMPLE.IN` | Lower-cased to varun.saxena21@example.in |
| `source2:15 · email_id` | `DEEPAK.NAIR44@EXAMPLE.COM` | Lower-cased to deepak.nair44@example.com |

_6 more in `reports/data_issues.csv`._

### 10. People matched on name + city with no shared identifier

`matched_on_name_and_city` · **5** occurrences

**The problem.** source2 has no phone column and source3 has no email column, so they can never be linked directly. A handful of people appear in both and in neither source1 - for them, name + city is the only signal available.

**What I did.** Merged at **medium** confidence, never high, and only when exactly one email-only and one phone-only cluster answer to that name+city. Every such merge is listed below with its exact source rows.

| Where | Value | Action |
|---|---|---|
| `source3:28` | `arjun mehta / Noida` | Merged at medium confidence |
| `source3:29` | `bhatia manish / Noida` | Merged at medium confidence |
| `source3:30` | `chopra divya / Noida` | Merged at medium confidence |

_2 more in `reports/data_issues.csv`._

### 11. Completely empty row

`blank_row` · **1** occurrence

**The problem.** An empty row would otherwise become a person with no name and no identifiers.

**What I did.** Dropped. Note the CSV parser is configured with `skip_empty_lines: false` deliberately, so the blank row is *reported* rather than silently swallowed.

| Where | Value | Action |
|---|---|---|
| `source2:12` | `` | Row dropped |

### 12. More than one email address survives onto one person

`multiple_emails_one_person` · **1** occurrence

**The problem.** Once the above merge happens, a choice has to be made about which address is canonical.

**What I did.** The shorter, unprefixed address wins. Every address is retained as an identifier, so nothing is lost.

| Where | Value | Action |
|---|---|---|
| `source1:27 · email` | `alt.nikhil.chopra70@example.com / nikhil.chopra70@example.com` | Primary set to nikhil.chopra70@example.com, all addresses kept in person_identifiers |

### 13. Given name abbreviated to an initial

`name_abbreviated` · **1** occurrence

**The problem.** "R. Verma" cannot safely drive a name-based match - it would match every Verma in the file.

**What I did.** Single-letter tokens are excluded from the match key. Such a row can be *pulled into* a match by a stronger signal, but can never *cause* one.

| Where | Value | Action |
|---|---|---|
| `source1:25 · Full Name` | `R. Verma` | Excluded initials from the match key |

### 14. A third person shares the same name and city

`name_city_shared_with_third_person` · **1** occurrence

**The problem.** Two different people called Arjun Mehta both live in Noida. This is the trap in the dataset: a naive name+city rule merges them and destroys a person.

**What I did.** A cluster that already holds both an email and a phone cannot be the missing half of a bridge, so it does not block the merge - but it could still own the row via a second email address, so skills are checked as corroborating evidence. They differ, so it is ruled out and the decision is recorded.

| Where | Value | Action |
|---|---|---|
| `source3:28` | `arjun mehta / Noida` | Merged the email-only and phone-only rows; the complete cluster was ruled out |

## Low severity

### 15. Five phone formats for the same number

`phone_format` · **60** occurrences

**The problem.** `+919000000254`, `9000000237`, `09000000287`, `919000000231`, `+91-9000000131`. These are the join key between source1 and source3 - source3 has no email at all - so inconsistent formatting means the two files can never be linked.

**What I did.** Stripped to the 10-digit core and stored as E.164 (`+91XXXXXXXXXX`). One issue is logged per defective value, not one per defect.

| Where | Value | Action |
|---|---|---|
| `source1:3 · Phone` | `9000000237` | Normalised to +919000000237 |
| `source1:4 · Phone` | `09000000287` | Normalised to +919000000287 |
| `source1:5 · Phone` | `9000000113` | Normalised to +919000000113 |

_57 more in `reports/data_issues.csv`._

### 16. Five date formats in one column

`date_format` · **33** occurrences

**The problem.** `2026-08-08`, `24-07-2026`, `07/13/2026`, `7 Jul 2026`, `19 Jul 2026` all appear in the same field.

**What I did.** Parsed with an explicit format per shape and strict matching, so an unexpected shape fails loudly instead of being guessed at. Stored as ISO-8601.

| Where | Value | Action |
|---|---|---|
| `source1:2 · Applied Date` | `24-07-2026` | Parsed as DD-MM-YYYY -> 2026-07-24 |
| `source1:4 · Applied Date` | `01-08-2026` | Parsed as DD-MM-YYYY -> 2026-08-01 |
| `source1:5 · Applied Date` | `7 Jul 2026` | Parsed as D MMM YYYY -> 2026-07-07 |

_30 more in `reports/data_issues.csv`._

### 17. Verified flag uses five different spellings

`boolean_inconsistent` · **30** occurrences

**The problem.** `Y`, `y`, `yes`, `Yes`, `N`, `No` in one column.

**What I did.** Normalised to 1 / 0, enforced by a CHECK constraint.

| Where | Value | Action |
|---|---|---|
| `source3:2 · Verified` | `Y` | Normalised to 1 |
| `source3:3 · Verified` | `yes` | Normalised to 1 |
| `source3:4 · Verified` | `No` | Normalised to 0 |

_27 more in `reports/data_issues.csv`._

### 18. Same city under several spellings

`city_variant` · **26** occurrences

**The problem.** `GURGAON` / `gurugram ` / `Gurugram`, and `bangalore` / `Bengaluru` - case differences plus genuine post-rename synonyms. City is part of the tier-3 match key, so unresolved variants cause missed matches.

**What I did.** Mapped to a canonical set. **`Delhi NCR` is collapsed to `Delhi`, and that is lossy** - NCR is a region that also contains Noida and Gurugram. It is flagged in the log rather than presented as a clean mapping.

| Where | Value | Action |
|---|---|---|
| `source1:3 · City` | `GURGAON` | Mapped to "Gurugram" |
| `source1:4 · City` | `GURGAON` | Mapped to "Gurugram" |
| `source1:5 · City` | `GURGAON` | Mapped to "Gurugram" |

_23 more in `reports/data_issues.csv`._

### 19. Status field is not a real enum

`status_case` · **22** occurrences

**The problem.** `Active`, `active`, `ACTIVE`, `Inactive`, `paused` all appear.

**What I did.** Normalised to `active` / `inactive` / `paused`, with `unknown` for anything absent, and enforced by a CHECK constraint in the schema.

| Where | Value | Action |
|---|---|---|
| `source2:2 · status` | `Active` | Lower-cased to "active" |
| `source2:3 · status` | `Active` | Lower-cased to "active" |
| `source2:5 · status` | `ACTIVE` | Lower-cased to "active" |

_19 more in `reports/data_issues.csv`._

### 20. Trailing whitespace in city names

`city_whitespace` · **14** occurrences

**The problem.** `"Noida "` and `"Noida"` are different strings and group separately.

**What I did.** Trimmed. Note the loader deliberately does not trim on read, so these are reported instead of being invisibly cleaned.

| Where | Value | Action |
|---|---|---|
| `source1:10 · City` | `gurugram ` | Trimmed to "gurugram" |
| `source1:13 · City` | `Noida ` | Trimmed to "Noida" |
| `source1:15 · City` | `Noida ` | Trimmed to "Noida" |

_11 more in `reports/data_issues.csv`._

### 21. Names in ALL CAPS

`name_case` · **9** occurrences

**The problem.** `RITU SHARMA` and `Ritu Sharma` are the same person; unnormalised they produce different match keys.

**What I did.** Re-cased to Title Case for display; the match key is lower-cased and token-sorted so word order does not matter either.

| Where | Value | Action |
|---|---|---|
| `source3:3 · Name` | `RITU SHARMA` | Re-cased to "Ritu Sharma" |
| `source3:7 · Name` | `RAHUL MALHOTRA` | Re-cased to "Rahul Malhotra" |
| `source3:9 · Name` | `SAHIL MALHOTRA` | Re-cased to "Sahil Malhotra" |

_6 more in `reports/data_issues.csv`._

### 22. Same person spelled differently across sources

`name_variants_merged` · **1** occurrence

**The problem.** Names arrive as ALL CAPS, Title Case, and abbreviated.

**What I did.** The most complete variant wins the golden record; all variants stay in person_sources.

| Where | Value | Action |
|---|---|---|
| `source1:31 · name` | `R. Verma / Rohit Verma` | Kept the most complete form "Rohit Verma" |

---

## Deliberately not decided

Cases where guessing would have been worse than admitting uncertainty. These are
written to `reports/review_queue.json` with their exact source rows so a human can
settle them.

- Same name, different city (e.g. the two Deepak Nairs, Bengaluru vs Delhi). Treated
  as different people: a shared name is not evidence of identity, and merging them
  would silently destroy one.

---

## What is NOT a data issue

Worth stating, because these look like defects and are not:

- **People appearing in only one file.** 23 of 55 are single-source. That is expected —
  the three systems have genuinely different populations, not missing data.
- **source2 having no phone column and source3 having no email column.** That is the
  shape of the problem, not a fault. It is why source1 is the only bridge between them,
  and why a name+city tier had to exist at all.
- **Two people sharing a name.** There really are two Arjun Mehtas and two Deepak Nairs.
  Collapsing them would be the bug.

